import React from 'react';

import { SharedWidgets } from '../../shared';

const DishesWidgets = () => {
	return (
		<SharedWidgets>
			<h1>Dishes Widgets</h1>
		</SharedWidgets>
	);
};

export default DishesWidgets;
