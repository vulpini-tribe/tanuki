import styled from 'styled-components';

export default styled.div`
	display: flex;
	flex-direction: column;
	height: 100%;
	min-height: 100vh;
	padding: var(--space-5) var(--space-5) calc(var(--space-5) + 60px) var(--space-5);
`;
