import styled from 'styled-components';

export default styled.div`
	position: fixed;
	top: var(--space-3);
	bottom: var(--space-3);
	left: calc(75px + var(--space-3) + var(--space-3));
	width: 350px;
	padding: var(--space-3);
	background-color: var(--slate-2);
	border-radius: var(--radius-3);
`;
